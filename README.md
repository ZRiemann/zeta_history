# zeta_history
History application. ZetaX subproject. 

Current version: `v0.1.0`. See [release history](release_history.org).

# Build-entry applicability

This repository currently contains documentation, not a native build project.
It intentionally has no zbuild.py and no placeholder successful build targets.
When a real deliverable is introduced, follow the shared zetax-project build
contract in zsa/agents/zetax/skills/zetax-project/references/build-entrypoints.md.
